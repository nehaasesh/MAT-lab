name: project1
description: "LocalStock Finder - Find products in nearby shops."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  provider: ^6.0.5
  http: ^1.1.0
  geolocator: ^10.1.0
  flutter_map: 6.1.0
  latlong2: 0.9.1
  intl: ^0.18.1
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
